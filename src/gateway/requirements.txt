fastapi==0.115.0
uvicorn[standard]==0.30.0
httpx==0.27.0
redis==5.1.0
numpy==1.26.4
scikit-learn==1.4.1.post1
joblib==1.3.2
# pandas is deliberately absent: the old gateway built a DataFrame per request
# purely to reuse training-time code. Feature extraction is now pure
# python/numpy, which removes both the dependency and the per-request overhead.
