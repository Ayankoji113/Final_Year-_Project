numpy==1.26.4
scikit-learn==1.4.1.post1
joblib==1.3.2
# pandas/matplotlib/seaborn are not used by train.py or calibrate.py.
# torch was listed previously but never imported -- the autoencoder is
# implemented in NumPy (see common/autoencoder.py), so a ~2 GB dependency
# has been removed rather than carried.
